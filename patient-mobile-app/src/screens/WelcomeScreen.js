import React from 'react';
import { View, Text, Button, StyleSheet } from 'react-native';

export default function WelcomeScreen({ navigation }) {
  return (
    <View style={styles.container}>
      <Text style={styles.title}>Welcome to SOS Tourist Doctor!</Text>
      <Button title="Sign Up" onPress={() => navigation.navigate('Auth', { isSignUp: true })} />
      <Button title="Login" onPress={() => navigation.navigate('Auth', { isSignUp: false })} />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    padding: 20,
  },
  title: {
    fontSize: 24,
    marginBottom: 20,
    textAlign: 'center',
  },
});
